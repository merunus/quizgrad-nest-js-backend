export const avatarsPath = "/avatars";
export const wordsPath = "/wordsImages";
export const validImageFileFormatsRegex = /\.(jpg|jpeg|png|svg|gif)$/;

// Multer
export const MULTER_FILE_SIZE_LIMIT = 5 * 1024 * 1024;
